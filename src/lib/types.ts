export interface CTA {
  text: string
  href: string
}

export interface FormData {
  fullname: string;
  email: string;
  serviceType: string;
  zipCode: string;
  phoneNumber: string;
  additionalInfo: string;
}